# 裁決書：B3 / C3 / C2 / C4 下一步施工判斷

> 日期：2026-06-22 ｜ 性質：**高風險裁決 + 可施工規格** ｜ 依 [AGENTS.md](../AGENTS.md)：本檔為 context，非計算真值。
> 基準：HEAD `a73e5df`（Add Excel header alignment guardrails）；工作樹乾淨；72 passed / VALIDATION COMPLETE（僅既有 21 筆 phase 2L-A warning）。
> 證據來源：實際 code（`export/excel/*.py`、`core/code_aliases.py`、`tests/test_excel_header_alignment.py`），非 Markdown。

---

## 1. 裁決總表

| 候選 | 裁決 | 理由（程式碼證據） |
|---|---|---|
| **A. B3 欄序/用語一致化** | **拆成 B3a 准做 / B3b 暫緩** | 改名零風險；重排會撞硬編碼欄索引，現有 guardrail 只部分覆蓋 |
| **B. C3 parser integration** | **准做「opt-in 接縫」；不准自動接全域 parser** | `resolve_designation_alias` 預設 `alias_ref=None` 會掃所有 pattern，自動接會誤吃標準型號 |
| **C. C2 variant overlay** | **暫緩** | 無 `configs/variants/`、無 ACME L 表、無 PDF/golden；碰計算真值 |
| **D. C4 常數外部化 / material canonicalization** | **暫緩（HOLD）** | 碰計算真值與 warning baseline；無 PDF/golden 不准動 |

### 關鍵證據（B3 為何要拆）
- `查核-支撐明細`（`leader_sheets.py::_write_leader_detail_sheet`）用**位置式 `values` list** 寫值，並硬編碼欄索引：`col in (6,10,11)`、`column=10`、`col_formats={6,10,11}`、位置式 `widths=[...]`。**重排 = 這些索引全錯位。**
- `重量明細表`（`calculation_sheets.py::_write_calculation_basis_sheet`）為**混合式**：部分用 `.index("總重(kg)"/"組數"/"列型")` 反查（耐重排），但同時有 `vals[18..22]`、`column=20..23`、`col in (10,11,12)`、`col in (7,8)`、位置式 `_set_widths`。重排同樣高危。
- guardrail `test_excel_header_alignment.py`：用**單一非 pipe-shoe 樣本**（`51-1.1/2B`），逐名只檢約 5–7 欄（型號/來源圖號/流水號/輸入數量/輸入單位/數量/單位）。它能 pin「表頭==常數」，但**擋不住** `命中明細/材質判定/統計條件/品名/規格` 等未檢欄的重排錯位。

---

## 2. B3 的 5 個裁決問題

1. **來源圖號/流水號放前面是否刻意稽核導向？** → **是。** sheet 物件名 `"支撐統計明細（製表者查核）"`、副標寫「逐筆列出…來源」，是 `長官-支撐分類` 的追溯對應表。**保持原樣，不要移。**
2. **型號移更左？** → `重量明細表` 與 `重量分析` 型號**已是第 1 欄**；`查核明細` 型號在第 8 欄是稽核設計。**B3a 不做任何位移。**
3. **Type→型號類別、管徑(in)→管徑(吋)？** → **准。** 全庫無 `.index("Type")`/`.index("管徑(in)")`、無 test 以此為 key；唯一連動是 `column_roles.py` 的 key 必須同步改。
4. **哪些 sheet 改 / 不改？** → 見 §3 白名單。
5. **驗收？** → 見 §3 驗收。

---

## 3. B3a 施工單（准做，可直接照做）

> 範圍：**只改欄名用語，零重排、零改寫值順序。一個 commit。**

### 改名清單

| 檔案 | 舊 | 新 |
|---|---|---|
| `export/excel/headers.py` `PROJECT_HEADERS` / `_CALC_BASIS_HEADERS` / `LEADER_DETAIL_HEADERS` | `Type` | `型號類別` |
| `export/excel/headers.py` `LEADER_GROUP_DETAIL_HEADERS` / `LEADER_DETAIL_HEADERS` | `管徑(in)` | `管徑(吋)` |
| `export/excel/column_roles.py` `COLUMN_ROLE` key | `"Type"` | `"型號類別"`（role/visible 不變） |
| `export/excel/column_roles.py` `COLUMN_ROLE` key | `"管徑(in)"` | `"管徑(吋)"` |

### 可改檔案（白名單）
- `python_app/export/excel/headers.py`
- `python_app/export/excel/column_roles.py`
- `python_app/validate_tables.py`（**施工時新發現的必改項**：4 處硬編碼表頭斷言 `== "Type"`，於 line 215/279/303/328，須同步改 `== "型號類別"`。authority level 5 guardrail，漏改會讓 validate 轉紅）。

### ⚠ 施工後修正：原「0 處字面值參照」判斷不正確
實際掃描 active 程式，舊欄名 `"Type"` 另有參照：
1. **`validate_tables.py` 4 處表頭斷言** → 已納入白名單並同步改（見上）。
2. **2 處 summary 表頭 inline literal（本次未改，刻意留作獨立 opt-in）**：`calculation_sheets.py:319`（計算標準與假設的「Type 計算資料狀態彙整」）、`project_summary_sheet.py:167`（專案摘要）。這兩處的欄值是 `Type 51` 之類的 per-Type 標籤，與明細表 `型號類別` 欄性質不同；改它們屬「不可改 sheet」名單，需另行確認。**現況：明細表已用「型號類別」，這兩張 summary 仍顯示「Type」，存在用語不一致，建議後續 B3a-2 一併處理。**

### 不可改 / 不可碰
- 任何 `core/`、`configs/`、`data/`（零碰計算真值）。
- 不重排任何欄、不改 `values` 順序、不改 `_set_widths` 內容、不改 `col in (...)` / `column=N` 索引。
- `export/excel_export_legacy.py`（legacy，非 active，留著別動）。
- `ui/main_window.py:63` 的 UI 結果表 `"Type"`（屬高風險 god object，列為獨立 opt-in，不在 B3a）。
- `長官摘要 / 專案摘要 / 計算標準與假設 / 長官-支撐分類 / 材料合計 / 下料明細 / 下料圖示`。

### 必跑驗證（前後都跑）
```powershell
python -m compileall -q python_app
python python_app\validate_tables.py                       # 須 VALIDATION COMPLETE
python python_app\validate_tables.py | Select-String '^X'  # 須無輸出
python -m pytest -q                                         # 須維持 72 passed
```
人工：匯出一份實樣，**diff 只應出現表頭文字 `Type→型號類別`、`管徑(in)→管徑(吋)`，其餘欄序/值/格式全同**。

### 停損條件
- `validate_tables` 轉紅 / 新增 `^X` / phase 2L-A warning 從 21 筆變動 → 回退。
- `test_excel_header_alignment.py` 或 `test_column_roles.py` 轉紅 → 漏改 key，回退修正。
- 任一 sheet 出現欄序/值位移 → 回退（代表誤碰重排）。

---

## 4. B3b（重排，暫不准動）— 解鎖前置（缺一不可）

1. **先補強 guardrail**：新增測試，對**多型號樣本（含一個 pipe-shoe，如 Type 54）逐欄**斷言 header↔value 對齊（非只檢 5 欄）。
2. （建議）把 `_write_leader_detail_sheet` / `_write_calculation_basis_sheet` 的硬編碼 `col in (...)`、`vals[N]`、`column=20..23` 改為 `.index(欄名)` 反查，消除位置耦合。
3. 才可重排，且**只動 `重量明細表`**（把 trace 欄 `項次` 移到 `材質` 後）；**`查核-支撐明細` 維持稽核欄序，不重排**。

---

## 5. C3 opt-in 規格（准做；不接全域 parser）

- 薄接縫：`標準 parse 優先 → 失敗或「明確啟用」時才 resolve_designation_alias(value, alias_ref=...) → 取回標準 designation 丟回既有 parser`。
- **禁止**在 `parser.parse / get_type_code` 對所有輸入無條件呼叫 `resolve_designation_alias(alias_ref=None)`。
- 必附測試：alias 關閉時標準型號（`01-...`、`51-1.1/2B`）解析**逐筆不變**；開啟且 `alias_ref="type_01@acme"` 時 `ACME-PS01-...` 正規化成 `01-...` 走同一計算路徑。
- 現況：`core/code_aliases.py` resolver + `configs/code_aliases.json`（含 ACME 範例）已就緒，僅缺 opt-in 接縫與測試。

---

## 6. 暫緩項目所需的人類資訊

### C2 variant overlay
1. ACME（或目標設計公司）**完整 L 表 / 常數覆寫值**。
2. 對應**原圖 / PDF**（≥1 型號可人工對照驗算）。
3. `base_type_id` / `variant_id` / designation pattern 確認。
4. 第一版是否**只做常數/表格覆寫**（designation 格式覆寫留第二版）。

### C4 常數外部化 / material canonicalization
1. **指定一個**目標型號（高頻、表單純）。
2. 該型號 **PDF / golden** 作為「外部化後輸出一字不差」對照。
3. material canonicalization 屬禁區，需**逐項書面確認**範圍。

---

*裁決書結束 ｜ 依 AGENTS.md：本檔為 context，非計算真值 ｜ 2026-06-22*
