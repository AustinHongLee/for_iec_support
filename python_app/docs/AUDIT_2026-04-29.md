# 專案結構審核報告

**對象**：`for_iec_support / python_app`
**日期**：2026-04-29（最後更新：2026-04-30 AI Session 5 material cleanup）
**焦點**：基礎結構完整性、潛在風險、與你列出的四個痛點

---

## ⚡ 給下一位 AI 的快速銜接指南

**重要**：本文件在多個 AI session 間持續更新。閱讀此節可在 3 分鐘內理解現況。

### 已完成的工作（Phase 0 → Phase 5c-b）

| Phase | 狀態 | 內容摘要 | 關鍵檔案 |
|-------|------|---------|---------|
| Phase 0 | ✅ Done | `AnalysisEntry` 新增 `role: str`、`geometry: GeometryHints`（含 `HolePattern`）| `core/models.py` |
| Phase 1 | ✅ Done | M42 plate 螺孔資料寫入 `geometry.holes`，remark 仍保留（雙寫） | `core/m42.py`, `core/plate.py` |
| Phase 2 | ✅ Done | `display_remark` property；UI / 5 個 export 改讀 `geometry` | `core/models.py`, `export/csv_export.py`, `export/excel_export.py`, `ui/main_window.py` |
| Phase 3 | ✅ Done | `ComponentRole` enum，`ROLE_AGGREGATE_TYPE` 字典，聚合 key 改用 role | `core/component_roles.py`, `core/material_summary.py` |
| Phase 4 | ✅ Done | `data/typeXX_table.py` → `configs/typeXX.json` bridge 模組；float key bug 修復（13 個 bridge 檔）| `data/type51_table.py` 等 13 個檔 |
| Phase 5 | ✅ Done | Pipe Shoe 家族（52/53/54/55/66/67/85）宣告式 TypeSpec | `configs/pipe_shoe_spec.json`, `core/pipe_shoe_engine.py`, `core/types/type_52.py` |
| Phase 5b | ✅ Done | `trunnion_engine` 材料常數共用（type_39/42/43/56）+ 20 個 golden cases（type_27/39/42/43/56）| `core/trunnion_engine.py`, `validate_tables.py` |
| Phase 5c-a | ✅ Done | `validate_tables.py` 保留原邏輯，新增 pytest wrapper + dev requirements | `pytest.ini`, `python_app/tests/test_validate_tables.py`, `python_app/requirements-dev.txt` |
| Phase 5c-b | ✅ Done | 共用 `material_specs.py`，移除 type calculators 內重複 `_material_spec` 樣板 | `core/material_specs.py`, `core/types/type_01.py` 等 |
| Phase 5c-c | ✅ Done | 新增 Type 定義/定位 contract 與 locator tool，說明並查詢 calculator、JSON、bridge、doc、test 對應規則 | `docs/TYPE_DEFINITION_CONTRACT.md`, `tools/find_type.py` |
| Phase 5d | ✅ Done | TypeSpec 第二批 pilot：Type 60 改為 `table_plate_v1`；Type 58 改為 `table_parts_v1` 並修正 JSON key lookup | `configs/type_58.json`, `configs/type_60.json`, `core/type_spec_engine.py` |
| Phase 5e | ✅ Done | Type 48/57 改為 `table_parts_v1`，支援 material suffix 與 M-26 row provider | `configs/type_48.json`, `configs/type_57.json`, `core/type_spec_engine.py` |
| Phase 5f | ✅ Done | 強化計算資料 anchor 規則：`support_ontology.json` 只算 metadata；新增 audit 工具抓出 calculator-only Type 風險 | `docs/TYPE_DEFINITION_CONTRACT.md`, `tools/find_type.py`, `tools/audit_table_json_coverage.py` |
| Phase 5g | ✅ Done | 新增 `type_anchor_index.json`，把 01T alias 與 pipe shoe shared spec 入口從工具 hardcode 改成可搜尋索引 | `configs/type_anchor_index.json`, `tools/find_type.py` |
| Phase 6a | ✅ Done | M42/M42A/M43 人眼判讀規則文件化，新增缺表尺寸 fallback + warning，鎖定 L40 角鐵 x2 / expansion bolt x4 | `docs/M42_BASE_SUPPORT_RULES.md`, `data/m42_table.py`, `core/m42.py` |
| Phase 6b | In Progress | 建立 Steel / M42 Type 驗證矩陣，列出長官關切 Type 的 steel/M42 風險與第一批 03/05/06/08 實算輸出 | `docs/STEEL_M42_TYPE_VERIFICATION_MATRIX.md` |
| Phase 6c | ✅ Done | 修正 Type 03 垂直角鐵下料公式：H 加上 LR elbow center、20mm offset、supported line 半徑，並鎖 golden case | `core/types/type_03.py`, `validate_tables.py` |
| Phase 6d | ✅ Done | 修正 Type 05 垂直角鐵下料公式：H 扣除圖面 View A-A 15mm offset，並鎖 golden case | `core/types/type_05.py`, `validate_tables.py` |
| Phase 6e | ✅ Done | Type 06 對 H 值固定產生現場裁切預量 warning，避免採購/下料誤認為精準完成長 | `core/types/type_06.py`, `validate_tables.py` |
| Phase 6f | ✅ Done | 修正 Type 07 sliding elbow：共用 200mm offset、H 範圍改 1500~3500、固定 H 現場裁切 warning，並納入 matrix | `core/types/type_07.py`, `validate_tables.py` |

### 目前已知待確認事項

1. **Pad 厚度規則（≤8" 管）**：已確認保留 Sch10S 管壁厚規則（如 2"→2.11mm），不改。

2. **validate_tables.py 全綠**：Phase 0~5c-b 後 validate_tables.py 全部通過（含 golden cases）。
   每次重構後請先跑：`python validate_tables.py`；裝好 dev 依賴後也可跑 `pytest`

3. **Type 定義/定位規約**：新增 `docs/TYPE_DEFINITION_CONTRACT.md` 與 `tools/find_type.py`。以後新增或拆分 Type 時，先看 contract；需要查現況可跑 `python python_app/tools/find_type.py 51`。
   - `support_ontology.json` / `type_catalog.json` 是 metadata，不得當作計算資料 anchor。
   - `configs/type_anchor_index.json` 是共享 spec / alias 的入口地圖；要修 Type 66 可先搜這個檔。
   - M42 是共用高風險底座程序；先看 `docs/M42_BASE_SUPPORT_RULES.md` 再改 `core/m42.py` 或 `data/m42_table.py`。
   - 鋼構/M42 採購驗證進度看 `docs/STEEL_M42_TYPE_VERIFICATION_MATRIX.md`。
   - 全量檢查：`python python_app/tools/audit_table_json_coverage.py`，看 `calculator-only risk` 作為下一批 JSON 化 backlog。

4. **git 狀態（Session 5f 收尾時）**：本地 main 領先 origin/main，working tree 需以實際 `git status` 為準。
   - 未 push（刻意保留本地 commit 回溯點）
   - index.lock 問題：Session 3 中未發生；若遇到執行 `Remove-Item .git\index.lock -Force`
   - 最後兩個 commit：`refactor(phase5b): type_39 uses trunnion_engine materials; add type_39 golden cases` 與 `refactor(phase5b): type_56 uses trunnion_engine materials; add type_56 golden cases`

---

## 一、結論先說（TL;DR）

| 等級 | 議題 | 影響面 |
|------|------|--------|
| 🔴 Critical | `AnalysisEntry` 欄位太扁平，幾何/螺孔資訊被擠進 `remark` 字串 | 顯示、CAD 串接、單元測試 |
| 🔴 Critical | 零件命名沒有受控詞彙（`Lug_plate` / `LUG PLATE TYPE-C` / `LUG_PLATE_C` 三種寫法並存） | 採購聚合、CAD 自動產圖、語意搜尋 |
| 🟠 High | 標準表格 90% 都寫死在 `.py` 檔，`config_loader` 只有 Type 01 在用 | 標準更新體驗 |
| 🟠 High | 60+ 個 `type_XX.calculate()` 各自 inline 寫尺寸常數與 BOM 順序 | 維護心智負擔 |
| 🟡 Medium | `_classify_entry` 用 `entry.name.split("_")[0]` 推屬性，命名一變分組就壞 | 採購聚合脆弱 |
| 🟡 Medium | M42 字母 → 動作對應表是 lambda 散落式 | 規則演化困難 |

---

## 二、資料模型風險：扁平 `AnalysisEntry` ✅ Phase 0~2 已解決

### 已完成
- `core/models.py`：新增 `HolePattern`、`GeometryHints` dataclass，`AnalysisEntry` 加入 `geometry: GeometryHints`
- `display_remark` property：fallback 順序 `geometry.holes → geometry.notes_zh → remark`
- M42 plate：螺孔資料寫入 `entry.geometry.holes`
- UI + 5 個 export 改讀 `display_remark`

### 尚未做
- **Phase 1 完整度**：只有 M42 寫入 `geometry.holes`，其他 30+ 個 type 的 remark 仍是字串。  
  建議下一步：type_27（Column remark）、type_42（主梁備註）各自加上 `geometry.role`。
- **CAD/IFC 匯出**：尚未串接，但 geometry 欄位已就位。

---

## 三、命名/語意風險 ✅ Phase 3 已建立基礎

### 已完成
- `core/component_roles.py`：`ComponentRole` enum + `ROLE_AGGREGATE_TYPE` dict
- `core/material_summary.py`：聚合 key 改用 `(role, spec, material)`
- `FLAT_BAR` 的 aggregate type 已從 `"linear"` 改為 `"plate"`（pipe shoe context）

### 尚未做
- **30+ 個 type 檔**：尚未回填 `plate_role=` 參數，多數 entry 的 `role` 仍是空字串
- **建議下一步**：從使用量最高的 type_42 / type_27 開始，逐一補上 `plate_role`

---

## 四、標準表格更新體驗 ✅ Phase 4 已完成 bridge 層

### 已完成
- 9 個 bridge 模組（`data/type51_table.py` 等）：JSON → Python 的 float key bug 已修復
  - 修復模式：dict comprehension 三路轉型（int/float/str）
- `data/type62_table.py`、`type64_table.py`、`type79_table.py`：補入缺失 import

### 尚未做
- **schema 驗證**：`configs/_schema/` 目錄尚未建立
- **standard version 追蹤**：`configs/_schema/standard_version.json` 尚未建立
- **未遷移的 table**：部分 type 的計算表仍寫死在 `.py`，可逐批 migrate

---

## 五、計算邏輯結構 ✅ Phase 5 已完成 Pipe Shoe 家族 pilot

### Phase 5 架構說明（給下一位 AI）

```
python_app/
├── configs/
│   └── pipe_shoe_spec.json          ← D-80 sizing rules + 6 component 定義
├── core/
│   ├── pipe_shoe_engine.py          ← 薄 engine（265 行），讀 spec 並 dispatch
│   └── types/
│       └── type_52.py               ← 3 行 wrapper，dispatcher only
```

**type_52.py 的 dispatch 邏輯**（types 52/53/54/55/66/67 全走同一個 engine）：
```python
def calculate(fullstring: str) -> AnalysisResult:
    type_id = get_part(fullstring, 1)   # "52", "53", ... "67"
    return pipe_shoe_engine.calculate(fullstring, type_id)
```

**pipe_shoe_spec.json 的 sizing 規則**：
- `A_by_size`：gusset plate 寬度（按管徑分段）
- `B_by_size`：gusset plate 厚度
- `C_by_size`：結構件規格（`"200*100*5.5"` / `"200*200*8"` / `"FB12"`）
- `D_by_size`：預設 LOPS 長度
- `E_by_size`：≥10" pad 半延伸量
- `pad_t_fallback_by_size`：pad 厚度規則（目前 ≤8" 用 Sch10S 管壁厚，待確認）

**component 列表**（6 個）：
| id | 名稱 | when 條件 |
|----|------|-----------|
| `pad` | Pad_52Type | `pad_symbol != N/A` |
| `wedge` | Angle 40×40×5 | `angle_wedge`（52/53/54/55=True，66/67=False）|
| `beam` | H Beam | `C != FB12`（≤14" 管）|
| `fab_bottom` | FB_52Type_1 | `C == FB12`（≥16" 管）|
| `fab_web` | FB_52Type_2 | `C == FB12`（≥16" 管）|
| `gusset` | FB_52Type_3 | `pipe_size >= 10` |

**手動驗證 5 案例**（Phase 5 完成後全部通過）：

| Input | Pad | 結構件 | Wedge | Gusset |
|-------|-----|--------|-------|--------|
| `52-1/2B-A-150-200` | 無 | H200×100×5.5 | ✓ | — |
| `52-2B(P)-A(A)-130-500` | 2.11mm（Sch10S） | H200×100×5.5 | ✓ | — |
| `66-10B(P)-A-150-250` | 9mm | H200×200×8 | — | 4× |
| `66-1.1/2B(P)-A-150-150` | 2.11mm（Sch10S） | H200×100×5.5 | — | — |
| `52-20B(P)-A-200-600` | 12mm | FAB T-section | ✓ | 4× |

### 下一步：把 TypeSpec 推廣到其他高風險 type

**Phase 5b 已在 Session 3 全部完成**：

| 項目 | Type | 完成內容 |
|------|------|----------|
| ✅ | type_42/43 | `trunnion_engine.py` 共用層，減少～80 行重複代碼 |
| ✅ | type_39 | 接上 trunnion_engine 材料常數，移除 `_material_spec` 樣板 |
| ✅ | type_56 | 接上 trunnion_engine 材料常數，移除 `_material_spec` 樣板 |
| ✅ | type_27/39/42/43/56 | **20 個 golden cases** 鎖定 BOM 結構（5 branches for type_56，3 cases each for type_39/43，4 cases for type_42，5 cases for type_27）|

**`_material_spec` 樣板已從以下 4 個檔案完全移除**：type_39、type_42、type_43、type_56  
→ 全部改 import `core/trunnion_engine.py` 的材料常數（`STRUCTURAL_MATERIAL`、`SUPPORT_PLATE_MATERIAL`、`PLATE_LUG_MATERIAL`、`ANCHOR_BOLT_MATERIAL`）

**仍有 `_material_spec` 樣板的 type（下一輪評估）：**

| 優先 | Type 家族 | 原因 |
|------|----------|------|
| 🟡 低 | type_01（已有 JSON）| config_loader 本身就在用，可作 Phase 5 pattern 示範 |

---

## 六、其他結構性風險（簡列）

1. **`material_summary._classify_entry` 字串前綴判斷**  
   → Phase 3 已改用 `role`，但多數 entry 的 role 仍是空字串，fallback 仍靠名稱前綴。  
   → 待 Phase 3 完整度提升後自然解決。

2. **`m42.py` 的 lambda actions dict**  
   → 目前 25 條 lambda 仍保留，Phase 1 僅加了 `geometry.holes`。  
   → 建議改成 `configs/m42_actions.json` 資料驅動，但風險中等，不急。

3. **`pdf_export.py` 固定 11 欄**  
   → 未動，新增欄位仍需手動同步。

4. **`assets/Type/*.pdf` 版次 metadata**  
   → `type_catalog.json` 尚未加 `source_drawings`。

5. **測試覆蓋率**  
   → `validate_tables.py` 仍是目前唯一的系統性測試來源；Phase 5c-a 已加 pytest wrapper，下一步是把大型腳本逐段拆成原生 pytest cases。

---

## 七、優先順序建議（更新版）

### 已完成
✅ `AnalysisEntry` 加 `role` + `geometry.holes`  
✅ `data/typeXX_table.py` bridge 遷移 + float key 修復（13 個檔）  
✅ Pipe Shoe 家族宣告式 TypeSpec（52/53/54/55/66/67）  
✅ `trunnion_engine.py` 材料常數共用層建立  
✅ type_39/42/43/56 移除 `_material_spec` 樣板，改用 engine 常數  
✅ 20 個 golden cases（type_27/39/42/43/56），覆蓋全部 BOM 分支  
✅ `validate_tables.py` pytest wrapper（不改原驗證邏輯）
✅ `_material_spec` 重複樣板清理：新增 `core/material_specs.py`，type calculators 改用共用材料常數/helper
✅ Type 定義/定位 contract + locator tool：`docs/TYPE_DEFINITION_CONTRACT.md`, `tools/find_type.py`
✅ TypeSpec 第二批 pilot：Type 60 由 `table_plate_v1` 驅動；Type 58 由 `table_parts_v1` 驅動，calculator 都縮為 thin wrapper
✅ TypeSpec 第三批：Type 48 material suffix 與 Type 57 M-26 lookup 改由 `TYPE_SPEC` 驅動

### 建議下一個 AI session 做的事

**短期（1 session，低風險）**
- [ ] 將 `validate_tables.py` 內的大區塊逐段拆成原生 pytest cases
- [ ] 評估 type10/14/15/16/62/64/65 的 service-aware `_material(...)` helper 是否也能收斂成共用 context helper
- [ ] 挑第四批 table-driven Type 套用 `table_parts_v1`，優先找「table row → plate/custom/warning」形狀的 calculator

**中期（2~3 sessions，中等風險）**
- [ ] schema 驗證層：`configs/_schema/` + CI check
- [ ] M-5/6/7 Pipe Clamp 尺寸表轉錄（COMPONENT_TABLE_STATUS 中標示 partial-lookup）

**長期（架構演化）**
- [ ] CAD/IFC 匯出串接 `geometry.holes`
- [ ] `m42.py` lambda → 資料驅動
- [ ] i18n layer（name en/zh 分離）

---

## 八、附錄：`AnalysisEntry` 演化路徑（更新版）

| 階段 | 狀態 | 變更 | 風險 | 收益 |
|------|------|------|------|------|
| Phase 0 | ✅ **Done** | 新增 `role: str`、`geometry: GeometryHints` | 0（純加） | 開始能在新 type 試驗結構化 |
| Phase 1 | ✅ **Done** | M42 plate 孔位資料寫進 `geometry["holes"]`，remark 仍保留 | 低（雙寫） | UI 可以開始讀 geometry |
| Phase 2 | ✅ **Done** | UI / export 改成讀 `display_remark`，停止 parse remark | 中 | 顯示乾淨、CAD 可串接 |
| Phase 3 | ✅ **Done** | role 收斂為 `ComponentRole` enum，舊字串映射過去 | 中 | 採購聚合穩定、i18n 可行 |
| Phase 4 | ✅ **Done** | `data/typeXX_table.py` bridge + float key 修復（9 檔） | 中 | 標準更新只改資料 |
| Phase 5 | ✅ **Done (pilot)** | Pipe Shoe 家族宣告式 TypeSpec（52/53/54/55/66/67）| 高（但已完成）| 心智負擔大幅下降 |
| Phase 5b | ✅ **Done** | `trunnion_engine` 材料常數共用；type_39/42/43/56 移除重複樣板；20 個 golden cases | 中（已完成）| type_39/42/43/56 獲得回歸保護 |
| Phase 5c-a | ✅ **Done** | `validate_tables.py` → pytest wrapper；新增 dev requirements | 低 | 可用 `pytest` 做回歸入口 |
| Phase 5c-b | ✅ **Done** | 掃除 `_material_spec` 樣板；新增共用 material specs | 低 | 材料 resolver 入口集中 |
| Phase 5c-c | ✅ **Done** | 新增 Type 定義/定位 contract + locator tool | 低 | future agent 可定位 calculator/json/bridge/doc/test |
| Phase 5d | ✅ **Done** | Type 58/60 `TYPE_SPEC` pilots (`table_parts_v1`, `table_plate_v1`) | 低 | TypeSpec 不再只有 pipe shoe 家族 |
| Phase 5e | ✅ **Done** | Type 48/57 `TYPE_SPEC` migration | 低 | material suffix / M-26 provider 可資料驅動 |
| Phase 5f | 🔲 **Next** | 拆分原生 pytest cases；第四批 table-driven TypeSpec；評估 service-aware material helper 共用化 | 低 | 全面覆蓋 |
| Phase 6 | 🔲 **Future** | schema 驗證 + CI check | 低 | 回歸保護、標準合規 |

每個 phase 都可以單獨 ship、單獨回退。

---

## 九、重要的實作細節（給下一位 AI 的地雷清單）

1. **Edit 工具截斷大型檔案**  
   本 session 多次遇到 Edit 工具在修改大型檔案（validate_tables.py, pipe_shoe_engine.py）後截斷後半部。  
   **解法**：對 >500 行的檔案，優先用 bash `cat >` 或 Python script 整體重寫。

2. **pyc 快取無法刪除（Windows NTFS 掛載）**  
   `/sessions/.../mnt/` 下的 `.pyc` 以 `-rwx------` 模式掛載，sandbox 無法刪除。  
   **解法**：用 `py_compile.compile(...)` 強制重新編譯覆蓋；或 touch .py 讓 Python 認 stale。

3. **git index.lock 問題**  
   sandbox 內 git 操作有時留下 `index.lock`，需用戶在本機 PowerShell 執行 `Remove-Item .git\index.lock -Force`。

4. **float key 問題（bridge 模組）**  
   JSON 讀出的 dict key 是 `str`，但 `get_lookup_value()` 回傳 `float`，導致查詢 miss。  
   已修復的 9 個 bridge 檔用三路轉型 dict comprehension 解決。  
   若新增 bridge 模組，記得套用同樣模式。

5. **type_52 是 dispatcher，type_56 是獨立家族**  
   - `calculator.py` 中 52/53/54/55/66/67 全部指向 `type_52.calculate`
   - type_56 是 D-67 pipe stop，**不在** pipe shoe 家族，計算邏輯完全不同，**勿混淆**

---

*audit by Claude — 2026-04-29*  
*updated by Claude Session 2 — 2026-04-30 (Phase 5 complete)*
