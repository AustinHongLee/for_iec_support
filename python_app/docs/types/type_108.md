# Type 108 — 小口徑假管支撐

來源為中威 D-119/D-120；格式 `108-{line}B-{H/100}{G|J|R}-{A|B|C}[(A)|(S)]`。

同一 line size/H 可能對應兩種 Supporting Pipe B，系統不自動猜選；需用 `supporting_pipe_size` 明選。H 是組立高度，只有另給 `supporting_pipe_cut_length_mm` 才列直管備料重。FIG-C 的 FB65×9×210 可精算；upper fishmouth、lug/spacer 淨輪廓與材料邊界仍是加工 blocker。
