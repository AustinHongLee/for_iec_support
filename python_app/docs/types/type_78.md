# Type 78 — Small-Pipe Strap Anchor / Support

| 項目 | 內容 |
|---|---|
| 圖面 | D-93 / M-54 FIG.1 |
| 編號 | `78-{line_size}B[(A)]` |
| 範圍 | 3/4"~4" |
| 狀態 | 尺寸 lookup 完成；重量與加工展開阻擋 |

Type 78 使用 M-54 FIG.1。M-54 的 `B` 是成形後外側跨距，不是平板展開長；缺 bend allowance／展開基準時不得用 `B×C×T` 算重。

`(A)` 只觸發圖面「IF USED AS ANCHOR」的焊接註記，不代表額外 anchor BOM。系統保留尺寸與焊接資料，但 strap 在展開未證實前為零重量 reference。
