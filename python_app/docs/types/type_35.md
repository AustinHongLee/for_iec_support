# Type 35 — 支撐軌道 / 托條

> **2026-07-29 原圖複核更正**：三份 D-40 的 FIG-A / FIG-B 是同一支 member 的截面／安裝型式，不是單條／雙條。兩型數量都為 1；未表列的 FIG-B 直接停算，H 超限則採有限分級。

| 項目 | 內容 |
|------|------|
| 中文名稱 | 支撐軌道 / 托條 |
| 英文名稱 | Support Rail / Bracket |
| 圖號 | 35 |
| 適用範圍 | L50(A≤600), L75(A≤800/B≤900), C100(A≤1400/B≤900), C125(A≤1400/B≤900), C150(A≤1400,A only), H100(A≤1400), H150(A≤1400) |
| PDF | `35.pdf` |
| 狀態 | ✅ 已分析 |

---

## 系統本質

焊接在 EXISTING SURFACE 上的單支線性構件，FIG-A / FIG-B 為截面／安裝型式，無 M-42，全焊接。

FIG-A / FIG-B 均為 qty=1。H 代表構件長度。NOTE 2：L50/C150 僅 FIG-A。

---

## 編碼格式

```text
35-C125-05A  第二段=型鋼, 第三段=HH(×100)+末位字母(A/B)
```

---

## 核心運算邏輯

```text
FIG-A: 型鋼×1, Total=H; FIG-B: 型鋼×1, Total=H
```

---

## 設計重點

- 本型式不使用 `M-42`，也沒有額外下部構件或螺栓。
- `FIG-A` 與 `FIG-B` 都是一支 member，只是截面／安裝型式不同。
- `NOTE 2` 限制：`L50`、`C150` 僅允許 `FIG-A`。
- H 小幅超界為一般警示，較大差異為高風險；超過圖示上限 2 倍或超額逾 2000 mm 仍停止。

---

## 與相近 Type 的關係

| 類別 | 型式 | 說明 |
|------|------|------|
| 相近型式 | 依 `type_catalog.json` 與圖面家族比對 | 後續可再補上更精細的 family / ontology 關聯。 |

---

## 備註

- 本文件先依現有 `calculator`、`type_catalog.json` 與圖面可辨識資訊整理。
- 若後續需要進一步資料化，可再補 `幾何參數表`、`BOM 結構表`、`PDF note 摘要` 與 `限制條件矩陣`。
